{% extends "LoginLoginBundle:Default:index.html.twig" %}

{% block container %}
    
    {% if name is defined %}
    <div class="alert-info fade in">
        <h1>Hello {{name}} </h1>
    </div>
    <div>
        <a href="{{path('login_login_logout')}}">Log out</a>
    </div>
    {% endif %}
{% endblock %}